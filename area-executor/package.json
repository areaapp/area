{
    "name": "area-executor",
    "version": "1.0.0",
    "description": "A service to execute areas",
    "main": "src/index.js",
    "type": "module",
    "scripts": {
        "build": "webpack --mode production && chmod +x dist/executor",
        "test": "echo \"Error: no test specified\" && exit 1",
        "dev": "webpack --dev && chmod +x dist/executor"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "axios": "^0.19.2",
        "consola": "^2.11.3",
        "dotenv": "^8.2.0",
        "js-base64": "^2.5.2",
        "pg": "^7.18.1",
        "yargs": "^15.1.0"
    },
    "devDependencies": {
        "webpack": "^4.41.6",
        "webpack-cli": "^3.3.11"
    }
}
